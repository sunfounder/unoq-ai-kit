/*
 * AI Vision Recognition — Sketch (minimal)
 *
 * All AI processing runs on the Linux MPU via Python.
 * The sketch side is idle — object detection is handled
 * entirely by the VideoObjectDetection brick.
 */

void setup() {
    // Nothing needed
}

void loop() {
    delay(1000);
}
