/*
 * Camera Preview — Sketch (minimal)
 *
 * This project runs primarily on the Linux MPU (Python + WebUI).
 * The sketch side is minimal — the camera is controlled entirely
 * from Python, and the video feed is served through the WebUI brick.
 */

void setup() {
    // Nothing needed — camera is on the MPU side
}

void loop() {
    delay(1000);
}
