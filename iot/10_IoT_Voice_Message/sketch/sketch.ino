/*
 * IoT Voice Message — Python-only lesson.
 *
 * The sketch is intentionally empty: recording uses the carrier's
 * microphone (STT brick) and playback uses its speaker (TTS brick),
 * both driven directly from Python.
 */

void setup() {
}

void loop() {
}
