:orphan:

1. 从APP LAB导出的.zip文件多一层文件夹，就是名字全小写之后再嵌入一层

\Copy of Blink LED.zip\copy-of-blink-led\

如果是没有任何修改直接导入是没有问题的。

如果你对里面的文件做修改，再原样压缩后，就会显示app.yaml文件不存在。

需要将copy-of-blink-led的内容全部提到\Copy of Blink LED目录下，再重新压缩。

Cloud LLM输入API之后，需要等一分钟左右运行代码，否则又重新让填写


Arduino UNO Q的IO口输入输出电流是多少，需要用1K电阻